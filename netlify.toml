[build]
  command = "cd Remindrr && pnpm install && pnpm build"
  publish = "Remindrr/dist"
  node = "20"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
